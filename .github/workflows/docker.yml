name: Docker

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  docker:
    uses: qntx/workflows/.github/workflows/docker.yml@main
